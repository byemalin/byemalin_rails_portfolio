<main class="animationShow">
  <h3><%= @project.title %></h3>
   <p><%= @project.description %></p>

  <div class="videosContainer">
    <% @project.videos.each do |video| %>
      <% url = "https://player.cloudinary.com/embed/?cloud_name=dxvi2kqnz&public_id=development/#{video.key}"%>
      <iframe
        src="<%= url %>"
        frameborder="0"
        allow="fullscreen">
      </iframe>
    <% end %>
  </div>

</main>
